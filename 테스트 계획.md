# 테스트 계획서

## Phase 0: 환경 준비

### 0-1. .env 파일 설정

프로젝트는 심볼릭 링크를 통해 `envs/` 폴더의 환경 파일을 사용합니다.

```bash
cd E:\Project\Team_Project\OZ-Final\AH_02_06

# 개발 환경 (Docker Compose)
# .env -> envs/.local.env (심볼릭 링크)
```

**필수 입력 항목:**

| 변수 | 설명 | 예시 |
|------|------|------|
| `ENV` | 실행 환경 | `local`, `dev`, `prod` |
| `SECRET_KEY` | JWT 비밀키 (32자 이상) | `python -c "import secrets; print(secrets.token_hex(32))"` |
| `DB_PASSWORD` | PostgreSQL 비밀번호 | (팀 공유) |
| `KAKAO_CLIENT_ID` | 카카오 앱 REST API 키 | Kakao Developers 콘솔 |
| `KAKAO_CLIENT_SECRET` | 카카오 앱 Secret | Kakao Developers 콘솔 |

**환경별 URL 설정:**

| 변수 | local | dev (Docker) | prod |
|------|-------|--------------|------|
| `FRONTEND_URL` | `http://localhost:3000` | `http://localhost` | `https://도메인` |
| `API_BASE_URL` | `http://localhost:8000` | `http://fastapi:8000` | `http://fastapi:8000` |
| `KAKAO_REDIRECT_URI` | `http://localhost:3000/auth/kakao/callback` | `http://localhost/auth/kakao/callback` | `https://도메인/auth/kakao/callback` |
| `NEXT_PUBLIC_API_BASE_URL` | `http://localhost:8000` | (빈 문자열) | (빈 문자열) |
| `NEXT_PUBLIC_ENV` | `local` | `dev` | `prod` |

**카카오 디벨로퍼 설정:**

Redirect URI를 카카오 디벨로퍼 콘솔에 등록해야 합니다:
- 개발: `http://localhost/auth/kakao/callback`
- 프로덕션: `https://도메인/auth/kakao/callback`

### 0-2. Docker 실행
```bash
# 이미지 빌드 및 시작
docker compose up -d --build

# 상태 확인 (모든 서비스 healthy 확인)
docker ps --format "table {{.Names}}\t{{.Status}}"
```

**예상 결과:**
```
NAME        STATUS                   PORTS
redis       Up (healthy)             6379:6379
fastapi     Up (healthy)             8000:8000
ai-worker   Up (healthy)             -
nextjs-fe   Up (healthy)             3000:3000
nginx       Up (healthy)             80:80
```

### 0-3. DBeaver 연결 설정
| 항목 | 값 |
|------|-----|
| Host | 52.78.62.12 |
| Port | 5432 |
| Database | doseph_db |
| User | doseph_admin |
| Password | (팀 공유) |

---

## Phase 1: 헬스체크 테스트

### 1-1. API 헬스체크
| 테스트 | 조작 | 예상 결과 |
|--------|------|----------|
| Liveness | `curl http://localhost/api/v1/health` | `{"status": "healthy"}` |
| Readiness | `curl http://localhost/api/v1/health/ready` | `{"status": "ready"}` |
| Nginx 프록시 | 브라우저에서 `http://localhost/` 접속 | Next.js 랜딩 페이지 표시 |

---

## Phase 2: 인증 플로우 테스트

### 환경별 로그인 동작

| 환경 | 카카오 로그인 | 개발자 로그인 버튼 |
|------|--------------|-------------------|
| `local` | Mock 서버 (FastAPI 내장) | 표시 |
| `dev` | **실제 카카오 서버** | 표시 |
| `prod` | 실제 카카오 서버 | 숨김 |

### 2-1. 개발자 로그인 (local/dev 환경)

개발 편의를 위해 카카오 인증 없이 테스트 계정으로 로그인할 수 있습니다.

| 단계 | 조작 | 예상 결과 | DB 검증 |
|------|------|----------|---------|
| 1 | `http://localhost/login` 접속 | 로그인 페이지 표시, "개발자로 로그인 (dev)" 버튼 표시 | - |
| 2 | "개발자로 로그인" 버튼 클릭 | `/main`으로 이동 | `accounts` 테이블에 테스트 계정 생성/조회 |

> **참고**: `prod` 환경에서는 개발자 로그인 버튼이 표시되지 않습니다.

### 2-2. 카카오 로그인 - Mock (local 환경)

`ENV=local`일 때 Mock 카카오 서버를 사용합니다.

| 단계 | 조작 | 예상 결과 |
|------|------|----------|
| 1 | 카카오 로그인 버튼 클릭 | Mock 인증 페이지로 이동 (`/api/v1/mock/kakao/authorize`) |
| 2 | Mock 로그인 완료 | `/auth/kakao/callback` 처리 후 `/survey` 또는 `/main`으로 이동 |

### 2-3. 카카오 로그인 - 실제 (dev/prod 환경)

`ENV=dev` 또는 `ENV=prod`일 때 실제 카카오 서버를 사용합니다.

| 단계 | 조작 | 예상 결과 | DB 검증 |
|------|------|----------|---------|
| 1 | `http://localhost/login` 접속 -> 카카오 로그인 버튼 클릭 | 카카오 로그인 페이지로 이동 (`kauth.kakao.com`) | - |
| 2 | 카카오 계정으로 로그인 | `/auth/kakao/callback` 처리 후 `/survey`로 이동 | `accounts` 테이블에 새 레코드 생성 |
| 3 | `/survey` 페이지 확인 | 건강정보 설문 폼 표시 | - |

**DB 검증 쿼리 (accounts):**
```sql
SELECT id, auth_provider, provider_account_id, nickname, is_active, created_at
FROM accounts
ORDER BY created_at DESC
LIMIT 5;
```

### 2-4. 건강정보 설문 (프로필 생성)
| 단계 | 조작 | 예상 결과 | DB 검증 |
|------|------|----------|---------|
| 1 | 기본정보 입력: 나이, 성별, 키, 몸무게 | 다음 단계로 이동 | - |
| 2 | 생활습관 입력: 흡연, 음주 여부 | 다음 단계로 이동 | - |
| 3 | 질환/알레르기 선택 -> 완료 버튼 | `/main`으로 이동 | `profiles` 테이블에 SELF 프로필 생성 |

**DB 검증 쿼리 (profiles):**
```sql
SELECT id, account_id, relation_type, name, health_survey, created_at
FROM profiles
WHERE deleted_at IS NULL
ORDER BY created_at DESC
LIMIT 5;
```

### 2-5. 기존 사용자 로그인
| 단계 | 조작 | 예상 결과 |
|------|------|----------|
| 1 | 로그아웃 후 다시 카카오 로그인 | `/main`으로 바로 이동 (설문 스킵) |
| 2 | 브라우저 쿠키 확인 | `access_token` 쿠키 존재 |

---

## Phase 3: 메인 페이지 테스트

### 3-1. 메인 대시보드
| 테스트 | 조작 | 예상 결과 |
|--------|------|----------|
| 시간대별 인사 | `/main` 접속 | 현재 시간대에 맞는 인사말 표시 |
| 프로필 조회 | 페이지 로드 | 프로필 이름, 건강정보 표시 |
| 복약 현황 | 페이지 로드 | 오늘 복약 진행률 (약품 없으면 빈 상태) |

---

## Phase 4: 프로필 관리 테스트

### 4-1. 가족 프로필 추가
| 단계 | 조작 | 예상 결과 | DB 검증 |
|------|------|----------|---------|
| 1 | `/mypage` -> 가족 관리 -> 추가 버튼 | 프로필 추가 모달 표시 | - |
| 2 | 이름, 관계(PARENT/CHILD/SPOUSE/OTHER), 건강정보 입력 | 프로필 생성 완료 | `profiles` 테이블에 새 레코드 |
| 3 | 가족 목록 확인 | 새 프로필 표시 | - |

**DB 검증 쿼리:**
```sql
SELECT id, account_id, relation_type, name, created_at
FROM profiles
WHERE account_id = '<your_account_id>'
  AND deleted_at IS NULL
ORDER BY created_at DESC;
```

### 4-2. 프로필 수정/삭제
| 테스트 | 조작 | 예상 결과 | DB 검증 |
|--------|------|----------|---------|
| 수정 | 가족 프로필 편집 -> 이름 변경 -> 저장 | 변경된 이름 표시 | `profiles.name` 업데이트, `updated_at` 갱신 |
| 삭제 | 가족 프로필 삭제 버튼 -> 확인 | 목록에서 제거 | `profiles.deleted_at` 값 설정 (soft delete) |

---

## Phase 5: 챌린지 테스트

### 5-1. 챌린지 시작
| 단계 | 조작 | 예상 결과 | DB 검증 |
|------|------|----------|---------|
| 1 | `/challenge` 접속 | 추천 챌린지 목록 표시 (금연, 운동 등) | - |
| 2 | 금연 챌린지 -> 시작 버튼 | 진행중 챌린지에 추가 | `challenges` 테이블에 새 레코드 |

**DB 검증 쿼리:**
```sql
SELECT id, profile_id, title, target_days, completed_dates,
       challenge_status, started_date, created_at
FROM challenges
WHERE deleted_at IS NULL
ORDER BY created_at DESC
LIMIT 5;
```

### 5-2. 챌린지 일일 체크
| 테스트 | 조작 | 예상 결과 | DB 검증 |
|--------|------|----------|---------|
| 오늘 체크 | 진행중 챌린지 -> 체크 버튼 | 진행률 업데이트 | `completed_dates` JSONB에 오늘 날짜 추가 |

**DB 검증 쿼리:**
```sql
SELECT id, title, completed_dates,
       jsonb_array_length(completed_dates) as completed_count,
       target_days
FROM challenges
WHERE id = '<challenge_id>';
```

---

## Phase 6: AI 채팅 테스트

### 6-1. 채팅 세션 생성
| 단계 | 조작 | 예상 결과 | DB 검증 |
|------|------|----------|---------|
| 1 | `/chat` 접속 또는 챗봇 아이콘 클릭 | 채팅 화면 표시 | - |
| 2 | 첫 메시지 전송: "안녕하세요" | AI 응답 수신 | `chat_sessions` + `messages` 생성 |

**DB 검증 쿼리 (chat_sessions):**
```sql
SELECT id, account_id, profile_id, title, created_at
FROM chat_sessions
WHERE deleted_at IS NULL
ORDER BY created_at DESC
LIMIT 5;
```

**DB 검증 쿼리 (messages):**
```sql
SELECT m.id, m.session_id, m.sender_type,
       LEFT(m.content, 50) as content_preview, m.created_at
FROM messages m
JOIN chat_sessions cs ON m.session_id = cs.id
WHERE cs.deleted_at IS NULL AND m.deleted_at IS NULL
ORDER BY m.created_at DESC
LIMIT 10;
```

### 6-2. 연속 대화
| 테스트 | 조작 | 예상 결과 | DB 검증 |
|--------|------|----------|---------|
| 후속 질문 | "두통에 좋은 약이 뭐야?" 전송 | AI 응답 (약 추천/안내) | 같은 session_id에 메시지 추가 |

---

## Phase 7: OCR 테스트 (처방전 등록)

### 7-1. 이미지 업로드
| 단계 | 조작 | 예상 결과 |
|------|------|----------|
| 1 | `/ocr` 접속 | 이미지 업로드 화면 |
| 2 | 처방전 이미지 선택 (JPG/PNG) | 미리보기 표시 |
| 3 | 분석 시작 버튼 | `/ocr/loading`으로 이동, 진행률 표시 |

### 7-2. OCR 분석 결과
| 테스트 | 조작 | 예상 결과 |
|--------|------|----------|
| 분석 완료 | 대기 (최대 60초) | `/ocr/result`로 이동, 인식된 약물 목록 |
| 약물 확인 | 결과 목록 확인 | 약품명, 용법 등 표시 |

> **참고**: OCR은 외부 API(CLOVA OCR)를 사용하므로 .env에 `CLOVA_OCR_SECRET_KEY`, `CLOVA_OCR_INVOKE_URL` 설정 필요

---

## Phase 8: 마이페이지 테스트

### 8-1. 정보 조회
| 테스트 | 조작 | 예상 결과 |
|--------|------|----------|
| 프로필 정보 | `/mypage` 접속 | 닉네임, 관계, 건강정보 표시 |
| 가족 목록 | 스크롤 | 등록된 가족 프로필 표시 |
| 챌린지 현황 | 페이지 로드 | 진행중 챌린지 개수 표시 |

### 8-2. 로그아웃
| 테스트 | 조작 | 예상 결과 | DB 검증 |
|--------|------|----------|---------|
| 로그아웃 | 로그아웃 버튼 클릭 | 로그인 페이지로 이동 | `refresh_tokens.is_revoked = true` |

**DB 검증 쿼리:**
```sql
SELECT id, account_id, is_revoked, expires_at, created_at
FROM refresh_tokens
WHERE account_id = '<your_account_id>'
ORDER BY created_at DESC
LIMIT 5;
```

---

## 테스트 체크리스트 요약

| Phase | 테스트 항목 | 상태 |
|-------|------------|------|
| 0 | 환경 준비 (.env, Docker, DBeaver) | [ ] |
| 1 | 헬스체크 (API, Nginx) | [ ] |
| 2-1 | 개발자 로그인 (local/dev) | [ ] |
| 2-2 | 카카오 로그인 - Mock (local) | [ ] |
| 2-3 | 카카오 로그인 - 실제 (dev/prod) | [ ] |
| 2-4 | 건강정보 설문 | [ ] |
| 2-5 | 기존 사용자 로그인 | [ ] |
| 3 | 메인 대시보드 | [ ] |
| 4-1 | 가족 프로필 추가 | [ ] |
| 4-2 | 프로필 수정/삭제 | [ ] |
| 5-1 | 챌린지 시작 | [ ] |
| 5-2 | 챌린지 일일 체크 | [ ] |
| 6-1 | 채팅 세션 생성 | [ ] |
| 6-2 | 연속 대화 | [ ] |
| 7 | OCR 처방전 분석 | [ ] |
| 8-1 | 마이페이지 조회 | [ ] |
| 8-2 | 로그아웃 | [ ] |

---

## 트러블슈팅

| 증상 | 원인 | 해결                                                      |
|------|------|---------------------------------------------------------|
| 카카오 로그인 실패 (dev/prod) | KAKAO_CLIENT_ID 미설정 또는 Redirect URI 미등록 | .env 파일 확인, 카카오 디벨로퍼 콘솔에 Redirect URI 등록                |
| 개발자 로그인 버튼 안보임 | `NEXT_PUBLIC_ENV=prod` 설정됨 | dev 환경에서는 `NEXT_PUBLIC_ENV=dev`로 설정                     |
| ECONNREFUSED 에러 | Docker 네트워크 설정 오류 | `API_BASE_URL=http://fastapi:8000` 확인                   |
| DB 연결 오류 | DB_PASSWORD 오류 또는 네트워크 | 52.78.62.12 접근 가능 확인                                    |
| 502 Bad Gateway | FastAPI 아직 시작 안됨 | `docker compose logs fastapi` 확인                        |
| OCR 타임아웃 | CLOVA API 키 미설정 | .env에 `CLOVA_OCR_SECRET_KEY`, `CLOVA_OCR_INVOKE_URL` 확인 |
| AI 응답 없음 | OpenAI API 키 미설정 | .env에 `OPENAI_API_KEY` 확인                               |
| Mock 카카오 안됨 (dev 환경) | ENV=dev에서는 실제 카카오 사용 | Mock 테스트는 `ENV=local`에서 수행                              |

---

## 변경 사항별 Docker 반영 방법

### 변경 유형별 필요 작업

| 변경 유형                   | 필요 작업 | 명령어                            |
|-------------------------|----------|--------------------------------|
| 소스 코드 변경                | 없음 (자동 반영) | -                              |
| `.env` 환경변수 변경          | 컨테이너 재생성 | `docker compose up -d`         |
| `docker compose.yml` 변경 | 컨테이너 재생성 | `docker compose up -d`         |
| `Dockerfile` 변경         | 이미지 재빌드 | `docker compose up -d --build` |
| `package.json` 등 의존성 변경 | 이미지 재빌드 | `docker compose up -d --build` |

---

### 1. 소스 코드 변경 시 (재시작 불필요)

개발 환경에서는 **Hot Reload**가 설정되어 있어 코드 변경 시 자동 반영됩니다.

| 서비스 | Hot Reload 방식 | 볼륨 마운트 |
|--------|----------------|-------------|
| FastAPI | `uvicorn --reload` | `./app:/app/app` |
| Next.js | HMR (Hot Module Replacement) | `./medication-frontend:/app` |

```bash
# 코드 수정 후 저장하면 자동 반영
# 별도 명령어 불필요
```

---

### 2. .env 환경변수 변경 시

환경변수는 **컨테이너 생성 시점**에 로드되므로 컨테이너 재생성이 필요합니다.

```bash
# 권장: 변경 감지하여 필요한 서비스만 재생성
docker compose up -d

# 특정 서비스만 재생성
docker compose up -d fastapi
docker compose up -d frontend
docker compose up -d ai-worker
```

**주의**: `docker compose restart`는 환경변수를 반영하지 않습니다!

| 명령어                      | .env 반영 | 설명 |
|--------------------------|----------|------|
| `docker compose restart` | X | 프로세스만 재시작 |
| `docker compose up -d`   | O | 컨테이너 재생성 |

**서비스별 환경변수 매핑:**

| 환경변수 | 사용 서비스 |
|----------|-----------|
| `ENV`, `SECRET_KEY`, `DB_*`, `KAKAO_*` | fastapi |
| `NEXT_PUBLIC_*` | frontend |
| `OPENAI_API_KEY`, `CLOVA_*` | ai-worker |

---

### 3. docker compose.yml 변경 시

포트, 볼륨, 네트워크 등 컨테이너 설정 변경 시 재생성이 필요합니다.

```bash
docker compose up -d
```

---

### 4. Dockerfile 또는 의존성 변경 시

`Dockerfile`, `requirements.txt`, `package.json` 등 변경 시 이미지 재빌드가 필요합니다.

```bash
# 변경된 서비스만 재빌드
docker compose up -d --build

# 특정 서비스만 재빌드
docker compose up -d --build fastapi
docker compose up -d --build frontend

# 전체 재빌드 (클린)
docker compose down
docker compose up -d --build
```

---

### 5. 완전 초기화 (문제 발생 시)

캐시나 볼륨 문제로 정상 동작하지 않을 때:

```bash
# 컨테이너 + 볼륨 삭제 후 재빌드
docker compose down -v
docker compose up -d --build
```
